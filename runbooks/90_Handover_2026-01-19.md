# 引き継ぎ書（2026-01-19）

## 1. 目的
- チャット分断に耐える形で、運用の正本と状態記録を固定する。
- GitHub: 正本（Runbook/規約）
- Google Drive: 重いログ/export/snapshot
- VPS: 実体

## 2. できたこと
- GitHub repo 作成: https://github.com/sty19jp-spec/infra-state
- README/Runbook を追加し、コミット運用が開始できている。
- Google Drive に3フォルダ作成:
  - 01_NPM設定 Export
  - 02_XserverVPS 構成YAML
  - 03_最新 snapshot
- snapshot（docker ps 等）をDriveに保存済み。
- n8n: ログイン画面表示OK
- Dify: アクセスOK

## 3. 現在の問題
- ASR: https://asr.neurobase-lab.com/health が 502 Bad Gateway

## 4. 現時点の切り分け結果（事実）
- docker ps | grep -i asr => 出力なし（ASRコンテナなし/稼働なし）
- ss -lntp | grep 8000 => 出力なし（ASR待ち受けなし）
- /opt/asr に asr_server.py と venv が存在（ホストPython実行の構成）

## 5. 次にやること（優先順）
1) ASRを起動して 8000 を listen させる（手動起動→systemd化）
2) NPM から ASR の upstream 到達確認（502→404/200に変化させる）
3) 変更前後 snapshot をDriveに保存し、10_ReadOnly_Check.md に追記
4) 後続: UFW/Fail2ban/鍵整理をAnsible等で自動化検討

## 6. 注意点
- “Last login: …” の行はコマンドではないので貼り付けない（誤実行の原因になる）
- find の括弧はエスケープが崩れると構文エラーになる。Runbookのコピペ単位で実行する。
